{
  "name": "@labset-harness/local",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "env:up": "docker compose up -d",
    "env:stop": "docker compose stop",
    "env:down": "docker compose down",
    "server:dev": "yarn env:up && nodemon",
    "client:dev": "webpack serve"
  }
}
